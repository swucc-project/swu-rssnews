#import "../fragments/fragments.graphql"

query GetAllCategories {
    categories {
        ...CategoryFields
    }
}

query GetCategory($id: Int!) {
    category(id: $id) {
        ...CategoryFields
    }
}